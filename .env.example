# MarketPulse Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# These are used in the React/Vite frontend application
# All frontend variables must be prefixed with VITE_
# =============================================================================

# Supabase Project Configuration
VITE_SUPABASE_PROJECT_ID=your_project_id_here
VITE_SUPABASE_URL=https://your_project_id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_public_key_here

# =============================================================================
# EDGE FUNCTIONS ENVIRONMENT VARIABLES
# These should be set in Supabase Dashboard → Edge Functions → Settings
# DO NOT add these to .env.local (they are for server-side only)
# =============================================================================

# Supabase Connection (for Edge Functions)
# SUPABASE_URL=https://your_project_id.supabase.co
# SUPABASE_ANON_KEY=your_anon_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# AI Services
# LOVABLE_API_KEY=your_lovable_api_key_here
# TAVILY_API_KEY=your_tavily_api_key_here

# Optional: Gold Rate API (if using paid API)
# REACT_APP_GOLD_API_KEY=your_gold_api_key_here

# =============================================================================
# HOW TO GET YOUR CREDENTIALS
# =============================================================================

# Supabase Credentials:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings → API
# 4. Copy:
#    - Project URL → VITE_SUPABASE_URL
#    - Project API keys → anon/public → VITE_SUPABASE_PUBLISHABLE_KEY
#    - Project API keys → service_role → SUPABASE_SERVICE_ROLE_KEY (Edge Functions only!)
# 5. Go to Settings → General
# 6. Copy Reference ID → VITE_SUPABASE_PROJECT_ID

# Lovable API Key:
# 1. Sign up at https://lovable.dev
# 2. Go to Settings → API Keys
# 3. Create a new API key
# 4. Copy the key → LOVABLE_API_KEY

# Tavily API Key:
# 1. Sign up at https://tavily.com
# 2. Get API key from dashboard
# 3. Copy the key → TAVILY_API_KEY

# =============================================================================
# SECURITY NOTES
# =============================================================================

# ⚠️ IMPORTANT:
# - NEVER commit .env.local to Git
# - NEVER use SERVICE_ROLE_KEY in frontend code
# - ONLY use SERVICE_ROLE_KEY in Edge Functions
# - ALWAYS use PUBLISHABLE_KEY (anon key) in frontend
# - Keep your API keys secret and rotate them periodically
